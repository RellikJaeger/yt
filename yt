#!/bin/bash
for url in $*; do
	yt-dlp -o "%(title)s.%(ext)s" -f bestvideo[height\<=1080][ext=mp4]+bestaudio[ext=m4a]/best[ext=mp4]/best $url
    mkdir -p "/sdcard/Download"
    mv *.mp4 "/sdcard/Download"
done
